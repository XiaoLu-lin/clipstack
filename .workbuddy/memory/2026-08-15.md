# 2026-08-15

## clipstack 初始化 GitHub + CI 打包流水线

- 项目是 Tauri v2（Rust + React19 + Vite）剪贴板工具，之前**未初始化 git**。
- 关键坑：`pnpm-lock.yaml` 被写死指向小红书内网 npm 镜像
  `artifactory.devops.xiaohongshu.com`（462 处）。本机 pnpm 也走这个源且当前 504。
  → 已删除旧 lockfile，用 `--registry=https://registry.npmjs.org/` 重新生成（17s 成功，0 处内网引用）。
  → 不改的话 GitHub Actions runner 也拉不到包，CI 必失败。
- 升级了 `.github/workflows/build.yml`：
  - 矩阵 macOS(aarch64-apple-darwin) + Windows(x86_64-pc-windows-msvc)
  - push 到 main → 仅出 Artifact（Actions 里下载，30 天有效）
  - 推送 `v*` 标签 → `tauri-apps/tauri-action@v0` 自动发布 GitHub Release（永久下载）
  - 加了 `permissions: contents: write`
- 已 `git init` + 提交（2cfd972）+ `gh repo create clipstack --public` 推到
  **https://github.com/XiaoLu-lin/clipstack**（main 分支，origin 已设）。
- CI run 已触发：31879738947（in_progress，mac/win 并行编译 Rust，约 15-25 min）。
- 待确认：CI 是否成功产出 .dmg / .msi+.exe。
- 注意：当前打包是**未签名**产物，mac 有 Gatekeeper 拦截、win 有 SmartScreen 提示；
  若要做"直接可用"的下载，需补 Apple 签名+公证 或 Windows 代码签名（需证书/密钥）。
